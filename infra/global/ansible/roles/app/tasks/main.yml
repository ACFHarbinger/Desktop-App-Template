---
- name: Ensure app directory exists
  ansible.builtin.file:
    path: /opt/desktop-app-repo
    state: directory
    mode: "0755"

- name: Template environment file
  ansible.builtin.template:
    src: env.j2
    dest: /opt/desktop-app-repo/.env
    mode: "0600"
