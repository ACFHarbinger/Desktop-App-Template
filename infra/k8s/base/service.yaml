apiVersion: v1
kind: Service
metadata:
  name: desktop-app-repo
  labels:
    app: desktop-app-repo
spec:
  selector:
    app: desktop-app-repo
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
