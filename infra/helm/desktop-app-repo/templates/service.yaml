apiVersion: v1
kind: Service
metadata:
  name: {{ include "desktop-app-repo.name" . }}
  labels:
    {{- include "desktop-app-repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "desktop-app-repo.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
