[project]
name = "desktop-app-template"
version = "0.1.0"
description = "Root workspace manifest for Desktop-App-Template (C++ base, Python backend, PySide6 Qt GUI)."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
authors = [{ name = "ACFHarbinger" }]
dependencies = []

[tool.uv.workspace]
members = ["backend", "gui", "git"]

[tool.uv]
# Root has no code of its own — workspace pointer to backend/ and gui/.
package = false

